const transportationModes: string[] = ["Honda Civic", "Tesla Model S", "Toyota Crown", "Ford Mustang GT"];

for (let i = 0; i < transportationModes.length; i++) {
    const mode = transportationModes[i];
    console.log(`I would like to own a ${mode}.`);
}
